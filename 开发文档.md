# 四级必过 - 微信小程序CET-4词汇速记工具 开发文档

## 1. 项目概述

**小程序名称：** 四级必过
**小程序简称：** 必过
**小程序介绍：** 3393个四级真题高频词，按20年真题词频排序，最常考的排最前。每天20词，美音朗读，学完即测，间隔复习。不背废词，只背必考词，一次过四级。

基于微信小程序的CET-4词汇速记工具，以历年真题词频为核心排序依据，只收录最高频的1000词，帮助考生用最短时间拿下四级词汇。通过广告变现。

**当前版本范围：** CET-4 前1000高频词
**后续扩展：** 全量3393词、CET-6、考研词汇等

---

## 2. 技术选型

| 层级 | 选型 | 理由 |
|------|------|------|
| 前端 | 微信小程序原生开发 | 云开发集成最佳，无额外抽象层，官方文档完善，音频API兼容性最好 |
| 后端 | 微信云开发 | 免运维，自带数据库/存储/云函数，免费额度足够早期使用，广告SDK集成原生支持 |
| 数据库 | 云开发数据库（MongoDB） | 与云开发深度集成，JSON文档结构天然适合词条数据 |
| 音频存储 | 云开发云存储 / 腾讯云COS | 小程序包不放音频，全部远程加载+本地缓存 |
| 英文TTS | Edge TTS（开发阶段批量生成） | 免费，微软语音质量高，美音自然 |
| 中文TTS | Edge TTS（zh-CN-XiaoxiaoNeural） | 同一工具链，中文语音质量满足需求 |
| 词频分析 | Python + spaCy | 真题文本分词、词形还原、频次统计 |

---

## 3. 数据模型

### 3.1 词条结构（words集合）

```json
{
  "_id": "abandon",
  "word": "abandon",
  "phonetic": "/əˈbændən/",
  "meaning": "放弃",
  "phrase": "abandon the plan",
  "phrase_meaning": "放弃计划",
  "example": "He abandoned his dream.",
  "example_meaning": "他放弃了梦想。",
  "freq_rank": 1,
  "level": "CET4",
  "audio": {
    "word": "cloud://xxx/audio/words/abandon.mp3",
    "phrase": "cloud://xxx/audio/phrases/abandon_phrase.mp3",
    "example": "cloud://xxx/audio/examples/abandon_example.mp3",
    "meaning": "cloud://xxx/audio/meanings/abandon_meaning.mp3",
    "phrase_meaning": "cloud://xxx/audio/phrase_meanings/abandon_phrase_meaning.mp3",
    "example_meaning": "cloud://xxx/audio/example_meanings/abandon_example_meaning.mp3"
  }
}
```

### 3.2 用户学习记录（user_progress集合）

```json
{
  "_id": "自动生成",
  "openid": "用户openid",
  "level": "CET4",
  "learned_words": ["abandon", "ability", "..."],
  "daily_count": 20,
  "current_unit": 3,
  "review_queue": [
    {
      "word": "abandon",
      "last_learned": "2026-03-06",
      "review_dates": ["2026-03-07", "2026-03-09", "2026-03-13", "2026-03-20"],
      "next_review": "2026-03-07",
      "stage": 1
    }
  ],
  "stats": {
    "total_days": 15,
    "streak_days": 7,
    "total_learned": 300
  }
}
```

### 3.3 复习策略

不做主动提醒，仅在进入小程序时显示当日应复习的单词列表。

复习间隔采用固定周期：**1天 → 3天 → 7天 → 14天 → 30天**

判断逻辑：
```
每次进入小程序 → 查询 review_queue 中 next_review <= 今天 的词 → 显示复习数量角标
```

---

## 4. 词频分析流程

### 4.1 数据采集

```
来源：CET-4历年真题文本（1987年至今）
内容：阅读理解、完形填空、翻译题、听力原文
格式：整理为纯文本文件，按年份命名
存放：/data/raw_texts/cet4/
```

### 4.2 处理流程

```
原始真题文本
    ↓
Python + spaCy 分词
    ↓
词形还原（lemmatization）：goes→go, abandoned→abandon
    ↓
去除停用词（the, a, is, are 等）
    ↓
词频统计（Counter）
    ↓
与CET-4大纲词表取交集
    ↓
按频次降序排列
    ↓
输出带频次排名的词表
```

### 4.3 关键注意事项

- 义项选择：取该词在真题中最高频的义项，非词典默认第一义项
  - 例：address 真题高频义为"解决/处理"，非"地址"
  - 例：issue 真题高频义为"问题"，非"发行"
- 词组选择：从真题原文中提取该词实际出现的搭配
- 例句：控制在10词以内，优先从真题中改编简化

### 4.4 词频分布分析

基于20年CET-4真题文本统计，3393个有效词条的频次分布如下：

| 排名 | 词数 | 每词出现频次 | 累计覆盖率 | 说明 |
|------|------|-------------|-----------|------|
| 1-100 | 100词 | 84-506次，平均141次 | ~23% | 超高频核心词 |
| 101-300 | 200词 | 46-84次，平均62次 | ~40% | 高频词 |
| 301-500 | 500词 | 32-46次，平均37次 | **56%** | 前500词过半覆盖 |
| 501-1000 | 500词 | 17-32次，平均23次 | **75%** | 前1000词覆盖3/4 |
| 1001-2000 | 1000词 | 6-17次，平均11次 | ~90% | 中低频 |
| 2001-3393 | 1393词 | 1-6次，平均3次 | 100% | 低频长尾 |

**关键结论：前1000词覆盖真题75%词汇量，性价比最高。**

### 4.5 当前版本产出

| 指标 | 数量 |
|------|------|
| CET-4大纲词汇总量 | 3393词（已全部整理） |
| **当前版本收录** | **前1000高频词**（覆盖真题75%） |
| 音频文件 | 1000词 × 6 = 6000个mp3 |
| 后续扩展至全量 | 3393词（数据已就绪） |

### 4.6 学习周期预估

基于1000词、复习间隔1/3/7/14/30天的模型：

| 每日学习量 | 学完天数 | 含复习总周期 | 每日耗时 | 适用场景 |
|-----------|---------|-------------|---------|---------|
| 10词/天 | 100天 | ~130天 | 10-15分钟 | 长线备考 |
| **20词/天（默认）** | **50天** | **~80天** | **15-25分钟** | **常规备考（推荐）** |
| 30词/天 | 34天 | ~65天 | 25-35分钟 | 中期冲刺 |
| 50词/天 | 20天 | ~50天 | 40-60分钟 | 临考突击 |

说明：
- 每日耗时包含新词学习（6段音频 + 阅读）+ 复习 + 测试
- 复习量中期会累积，高峰期每天约有新词 + 30-50个复习词
- 已认识的词可直接标记"已掌握"跳过，实际进度因人而异
- 产品宣传口径：**每天15分钟，30天突破四级词汇**

---

## 5. 音频生成流程

### 5.1 生成内容

每个词条生成6个音频文件：

| 序号 | 内容 | 语言 | 语音 |
|------|------|------|------|
| 1 | 单词（如 abandon） | 英文 | en-US-JennyNeural（美音女声） |
| 2 | 中文释义（如 放弃） | 中文 | zh-CN-XiaoxiaoNeural |
| 3 | 词组（如 abandon the plan） | 英文 | en-US-JennyNeural |
| 4 | 词组中文（如 放弃计划） | 中文 | zh-CN-XiaoxiaoNeural |
| 5 | 例句（如 He abandoned his dream.） | 英文 | en-US-JennyNeural |
| 6 | 例句中文（如 他放弃了梦想。） | 中文 | zh-CN-XiaoxiaoNeural |

### 5.2 批量生成脚本

使用 edge-tts Python库批量生成：

```bash
pip install edge-tts
```

```python
import edge_tts, asyncio, json

async def generate_audio(text, voice, output_path):
    communicate = edge_tts.Communicate(text, voice)
    await communicate.save(output_path)

async def batch_generate(words_json_path):
    with open(words_json_path) as f:
        words = json.load(f)
    for w in words:
        await generate_audio(w["word"], "en-US-JennyNeural", f"audio/words/{w['word']}.mp3")
        await generate_audio(w["meaning"], "zh-CN-XiaoxiaoNeural", f"audio/meanings/{w['word']}_meaning.mp3")
        await generate_audio(w["phrase"], "en-US-JennyNeural", f"audio/phrases/{w['word']}_phrase.mp3")
        await generate_audio(w["phrase_meaning"], "zh-CN-XiaoxiaoNeural", f"audio/phrase_meanings/{w['word']}_phrase_meaning.mp3")
        await generate_audio(w["example"], "en-US-JennyNeural", f"audio/examples/{w['word']}_example.mp3")
        await generate_audio(w["example_meaning"], "zh-CN-XiaoxiaoNeural", f"audio/example_meanings/{w['word']}_example_meaning.mp3")

asyncio.run(batch_generate("data/words_cet4.json"))
```

### 5.3 存储与缓存策略

- 音频文件上传至云开发云存储
- 小程序端使用 `wx.downloadFile` + `wx.getFileSystemManager` 缓存已播放音频
- 按学习单元预加载：进入某单元时，后台下载该单元全部音频
- 缓存上限管理：保留最近5个单元的缓存，自动清理更早的

---

## 6. 功能模块

### 6.1 学习模块

**单词卡片浏览：**

```
┌──────────────────────────────┐
│         abandon              │  ← 大字显示
│      /əˈbændən/              │  ← 音标
│                              │
│  🔊 点击/自动播放             │
│                              │
│  播放顺序：                   │
│  英文单词 → 中文释义           │
│  → 英文词组 → 词组中文         │
│  → 英文例句 → 例句中文         │
│                              │
│  释义：放弃                   │
│  词组：abandon the plan       │
│       放弃计划                │
│  例句：He abandoned his dream.│
│       他放弃了梦想。           │
│                              │
│  [上一词]  [已掌握]  [下一词]  │
└──────────────────────────────┘
```

**播放模式切换：**
- 手动模式：点击播放按钮，播放当前卡片完整音频序列
- 连续模式：当前卡片播放完毕后自动翻到下一词并播放

**每日学习量：**
- 默认20词/天
- 用户可在设置中调整（10/20/30/50/100）

### 6.2 测试模块

**测试一：选择汉语解释**

```
┌──────────────────────────────┐
│  🔊 abandon                  │
│                              │
│  请选择正确的中文含义：        │
│                              │
│  A. 能力                     │
│  B. 放弃          ✓          │
│  C. 吸收                     │
│  D. 接受                     │
│                              │
│  [下一题]                     │
└──────────────────────────────┘
```

规则：
- 从当日已学单词中出题
- 4个选项，干扰项从同单元/同频段词汇中随机选取
- 答对标绿，答错标红并显示正确答案
- 记录正确率

**测试二：默写单词**

```
┌──────────────────────────────┐
│  释义：放弃                   │
│  🔊 播放发音                  │
│                              │
│  请输入英文单词：              │
│  ┌────────────────────────┐  │
│  │ a b a n d o n          │  │
│  └────────────────────────┘  │
│                              │
│  ✓ 正确!                     │
│                              │
│  [下一题]                     │
└──────────────────────────────┘
```

规则：
- 给出中文释义 + 可播放英文发音
- 用户键盘输入单词
- 不区分大小写
- 错误时显示正确拼写，标红错误字母
- 可选择"提示"显示首字母

### 6.3 复习模块

```
┌──────────────────────────────┐
│  今日复习                     │
│                              │
│  待复习：32 词                │
│                              │
│  复习采用与学习相同的卡片模式   │
│  每个词标记 [记住了] / [忘了]  │
│                              │
│  标记"忘了"的词重置复习周期    │
│  标记"记住了"的词进入下一轮    │
└──────────────────────────────┘
```

### 6.4 统计模块

```
┌──────────────────────────────┐
│  我的学习                     │
│                              │
│  已学单词：320 / 1500         │
│  连续打卡：7 天               │
│  今日已学：20 词              │
│  今日待复习：32 词            │
│  测试正确率：85%              │
│                              │
│  ████████░░░░  21.3%         │  ← 总进度条
└──────────────────────────────┘
```

### 6.5 设置模块

- 每日学习量调整（10/20/30/50/100）
- 播放模式切换（手动/连续）
- 发音速度调节（0.8x / 1.0x / 1.2x）
- 清除缓存
- 词汇等级切换（CET-4，后续扩展CET-6等）

---

## 7. 广告集成

### 7.1 广告位规划

| 广告类型 | 位置 | 触发时机 |
|---------|------|---------|
| Banner广告 | 学习统计页底部 | 常驻 |
| 插屏广告 | 完成每日学习任务后 | 完成当日全部新词学习 |
| 激励视频广告 | 测试模块"提示"功能 | 用户主动点击获取提示 |
| 激励视频广告 | 解锁额外每日学习量 | 超出当日免费量时 |

### 7.2 接入方式

使用微信小程序流量主功能：
- 小程序累计独立访客（UV）达1000后可开通
- 通过 `wx.createBannerAd` / `wx.createInterstitialAd` / `wx.createRewardedVideoAd` 接入
- 广告单元ID在微信公众平台后台创建

---

## 8. 项目目录结构

```
LearnE/
├── miniprogram/                   # 小程序前端
│   ├── app.js
│   ├── app.json
│   ├── app.wxss
│   ├── pages/
│   │   ├── index/                 # 首页（今日任务概览）
│   │   ├── learn/                 # 学习页（单词卡片）
│   │   ├── test/                  # 测试页
│   │   │   ├── choice/            # 选择汉语解释
│   │   │   └── spell/             # 默写单词
│   │   ├── review/                # 复习页
│   │   ├── stats/                 # 学习统计
│   │   └── settings/              # 设置
│   ├── components/
│   │   ├── word-card/             # 单词卡片组件
│   │   ├── audio-player/          # 音频播放控制组件
│   │   └── progress-bar/          # 进度条组件
│   ├── utils/
│   │   ├── audio.js               # 音频播放/缓存管理
│   │   ├── db.js                  # 数据库操作封装
│   │   └── review.js              # 复习周期计算
│   └── styles/
│       └── common.wxss            # 公共样式
├── cloudfunctions/                # 云函数
│   ├── login/                     # 用户登录
│   ├── getWords/                  # 获取词汇数据（分页）
│   ├── updateProgress/            # 更新学习进度
│   └── getReviewList/             # 获取今日复习列表
├── data/                          # 数据处理（不上传至小程序）
│   ├── raw_texts/                 # 真题原始文本
│   │   └── cet4/
│   ├── scripts/
│   │   ├── freq_analysis.py       # 词频分析脚本
│   │   ├── generate_words.py      # 生成词条数据
│   │   └── generate_audio.py      # 批量生成音频
│   ├── wordlists/
│   │   ├── cet4_syllabus.txt      # CET-4大纲词表
│   │   └── cet4_freq_ranked.json  # 频次排序后的词表
│   └── words_cet4.json            # 最终词条数据
├── audio/                         # 生成的音频文件（上传至云存储）
│   ├── words/
│   ├── meanings/
│   ├── phrases/
│   ├── phrase_meanings/
│   ├── examples/
│   └── example_meanings/
└── 开发文档.md                     # 本文件
```

---

## 9. 开发阶段规划

### 第一阶段：数据准备

- [ ] 收集整理CET-4历年真题文本
- [ ] 编写词频分析脚本，输出频次排名词表
- [ ] 人工审核高频1500词的义项、词组、例句
- [ ] 使用Edge TTS批量生成全部音频文件
- [ ] 数据导入云开发数据库，音频上传云存储

### 第二阶段：核心功能开发

- [ ] 小程序项目初始化 + 云开发环境配置
- [ ] 用户登录与进度存储
- [ ] 单词卡片页面 + 音频播放（手动/连续模式）
- [ ] 每日学习流程（领取任务 → 逐词学习 → 标记完成）
- [ ] 复习列表展示与复习流程

### 第三阶段：测试与统计

- [ ] 选择汉语解释测试
- [ ] 默写单词测试
- [ ] 学习统计页面
- [ ] 设置页面

### 第四阶段：上线与变现

- [ ] 广告位接入（Banner + 插屏 + 激励视频）
- [ ] 体验版测试
- [ ] 提交微信审核上线
- [ ] 积累UV至1000，开通流量主

### 第五阶段：扩展

- [ ] CET-6词汇数据
- [ ] 考研词汇数据
- [ ] 更多测试模式
- [ ] 排行榜/打卡分享

---

## 10. 关键技术要点备忘

### 音频播放队列实现

单词卡片的6段音频需顺序播放，核心逻辑：

```javascript
// utils/audio.js
const playSequence = (audioList) => {
  let index = 0;
  const ctx = wx.createInnerAudioContext();

  const playNext = () => {
    if (index >= audioList.length) {
      // 序列播放完毕
      if (isContinuousMode) {
        // 连续模式：触发翻页并播放下一词
        triggerNextCard();
      }
      return;
    }
    ctx.src = audioList[index];
    ctx.play();
    index++;
  };

  ctx.onEnded(playNext);
  ctx.onError((err) => {
    console.error('音频播放失败', err);
    index++;
    playNext(); // 跳过错误音频继续播放
  });

  playNext();
};
```

### 干扰项生成逻辑

选择题的干扰项应避免过于简单或过于相似：

```javascript
// 从同频段（±50排名）中随机选3个不同词的释义
// 排除与正确答案含义相近的词（需预标注近义词组）
// 排除与正确答案字形相似的词
```

### 默写判定逻辑

```javascript
// 不区分大小写
// 去除首尾空格
// 比对每个字母，标记错误位置用于红色高亮
const checkSpelling = (input, correct) => {
  const a = input.trim().toLowerCase();
  const b = correct.toLowerCase();
  if (a === b) return { correct: true };
  // 逐字符比对，返回差异位置
  const diff = [];
  for (let i = 0; i < Math.max(a.length, b.length); i++) {
    if (a[i] !== b[i]) diff.push(i);
  }
  return { correct: false, diff, expected: b };
};
```

---

## 11. 用户体验改进计划（2026-03-08 起）

### 11.1 改进背景

基于完整用户使用流程分析，识别出 8 个关键体验问题，按优先级分阶段实施。

### 11.2 问题清单与解决方案

| 优先级 | 问题 | 解决方案 | 预估成本 |
|--------|------|----------|----------|
| P0 | 首次用户体验断层，无引导 | 3 屏新手引导页 | 低 |
| P0 | 学习流程中"测试突袭" | 测试前明确提示弹窗 | 低 |
| P1 | 错题本功能鸡肋，只能查看 | 加入复习/专项测试/标记攻克 | 中 |
| P1 | 复习流程缺乏正向反馈 | 即时反馈动画 + 完成总结 | 低 |
| P1 | 统计页信息量不足 | 学习趋势图/预估完成/成就徽章 | 中 |
| P2 | 缺少中断恢复机制 | 学习进度自动保存与恢复 | 低 |
| P2 | 缺少社交分享激励 | 打卡分享海报生成 | 中 |
| P3 | 设置页功能单薄 | 复习提醒/数据备份/意见反馈 | 低 |

### 11.3 新手引导设计

**触发条件：** 首次启动小程序（`wx.getStorageSync('hasGuidance')` 为空）

**引导页内容（3 屏）：**

```
┌─────────────────────────┐
│    📚 四级必过          │
│                         │
│   每天 20 词            │
│   30 天掌握四级核心词汇   │
│                         │
│   ● ○ ○                 │
│       [开始]            │
└─────────────────────────┘

┌─────────────────────────┐
│    📝 学完即测          │
│                         │
│   每个单词学习后        │
│   自动进入测试环节      │
│   答对即掌握            │
│                         │
│   ○ ● ○                 │
│       [继续]            │
└─────────────────────────┘

┌─────────────────────────┐
│    🔄 间隔复习          │
│                         │
│   1 天→3 天→7 天→14 天→30 天 │
│   科学对抗遗忘曲线      │
│                         │
│   ○ ○ ●                 │
│       [开始学习]        │
└─────────────────────────┘
```

**实现文件：**
- `pages/guide/guide.js/wxml/wxss/json`

### 11.4 测试前提示设计

**触发时机：** 用户点击"开始测试"按钮后，正式进入选择题前

**弹窗内容：**
```
┌─────────────────────────┐
│   📝 学完测试时间        │
│                         │
│   abandon               │
│   /əˈbændən/           │
│                         │
│   准备好了吗？          │
│                         │
│   [开始测试]  [跳过]    │
└─────────────────────────┘
```

**跳过逻辑：** 标记该词为"待复习"，加入复习队列

### 11.5 错题本功能增强

**新增功能：**

1. **加入复习** — 将错题加入今日复习队列
2. **专项测试** — 只测试该日期/该单词
3. **标记攻克** — 从错题本移除

**数据模型变更：**
```javascript
// error_book 结构升级
{
  "2026-03-08": [
    {
      "word": "abandon",
      "meaning": "放弃",
      "type": "choice",
      "userAnswer": "能力",
      "correctAnswer": "放弃",
      "timestamp": 1741536000000,
      "mastered": false  // 新增：是否已攻克
    }
  ]
}
```

### 11.6 复习流程反馈增强

**即时反馈：**
```javascript
// 用户点击"记住了/忘了"后显示短暂动画
if (remembered) {
  showFeedback('🎉 太棒了!', 'success')
} else {
  showFeedback('💪 下次一定行!', 'encourage')
}
```

**完成总结：**
```
┌─────────────────────────┐
│   复习完成！🎉          │
│                         │
│   本次复习 32 词         │
│   ✅ 28 词已掌握         │
│   ⚠️ 4 词需要再次复习    │
│                         │
│   [返回首页]            │
└─────────────────────────┘
```

### 11.7 统计页增强

**新增模块：**

1. **近 7 天学习趋势** — 简易柱状图
2. **预计完成时间** — 按当前进度估算
3. **成就徽章** — 里程碑奖励

**成就徽章列表：**
| 徽章 | 解锁条件 |
|------|----------|
| 初学者 | 学习第 1 个单词 |
| 坚持 3 天 | 连续打卡 3 天 |
| 坚持 7 天 | 连续打卡 7 天 |
| 坚持 30 天 | 连续打卡 30 天 |
| 百词斩 | 累计学习 100 词 |
| 五百词 | 累计学习 500 词 |
| 千词达人 | 累计学习 1000 词 |
| 测试王者 | 测试正确率 100% |

### 11.8 设置页完善

**新增设置项：**

```
设置
├── 学习设置
│   ├── 每日学习量
│   ├── 播放模式
│   ├── 发音速度
│   └── 复习提醒 [新增]
├── 数据管理
│   ├── 缓存大小
│   ├── 清除本地缓存
│   ├── 备份学习数据 [新增]
│   └── 恢复学习数据 [新增]
├── 关于
│   ├── 版本号
│   ├── 词库
│   └── 意见反馈 [新增]
```

### 11.9 中断恢复机制

**保存时机：**
- 每次完成一个词的学习（通过测试后）
- 页面 onHide 时

**恢复逻辑：**
```javascript
// 30 分钟内的中断可恢复
const temp = wx.getStorageSync('temp_learning_progress')
if (temp && Date.now() - temp.timestamp < 30 * 60 * 1000) {
  // 提示用户是否恢复
}
```

### 11.10 打卡分享海报

**海报元素：**
- 小程序名称/Logo
- 用户学习数据（连续打卡天数、已学词数）
- 激励文案
- 小程序码

**触发时机：**
- 完成每日学习任务后
- 连续打卡里程碑（3/7/30 天）

---

## 12. 开发阶段规划（更新）

### 第六阶段：用户体验优化（进行中）

- [x] 分析完整用户流程，识别体验问题
- [ ] P0: 新手引导页面
- [ ] P0: 测试前提示弹窗
- [ ] P1: 错题本功能增强
- [ ] P1: 复习流程反馈增强
- [ ] P1: 统计页增强
- [ ] P2: 中断恢复机制
- [ ] P2: 打卡分享海报
- [ ] P3: 设置页完善

### Bug 修复（2026-03-08）

| 问题 | 原因 | 修复方案 |
|------|------|----------|
| 新手引导按钮无响应 | `redirectTo` 不能用于 tabBar 页面 | 改用 `wx.switchTab()` |
| 英文发音两次 | `autoplay=true` 与手动 `play()` 冲突 | 设置 `autoplay=false`，显式调用 `play()` |
| 连续播放进入测试 | 逻辑设计错误 | 修改为自动播放下一个单词 |

### 第七阶段：上线与变现（待开始）

- [ ] 广告位接入（Banner + 插屏 + 激励视频）
- [ ] 体验版测试
- [ ] 提交微信审核上线
- [ ] 积累 UV 至 1000，开通流量主

### 第八阶段：扩展（待开始）

- [ ] CET-6 词汇数据
- [ ] 考研词汇数据
- [ ] 更多测试模式
- [ ] 排行榜/打卡分享
